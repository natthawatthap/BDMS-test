import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    padding: 16,
  },
  questionCount: {
    fontSize: 18,
    marginBottom: 12,
  },
  questionText: {
    fontSize: 20,
    fontWeight: 'bold',
    marginBottom: 24,
    textAlign: 'center',
  },
  buttonContainer: {
    marginVertical: 8,
  },
  button: {
    borderWidth: 1,
    borderColor: 'black', 
  },
});
